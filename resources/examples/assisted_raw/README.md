# 从原始结构开始示例

这个示例用于体验 DockStart 的辅助模式：从 `raw/receptor.pdb` 和 `raw/ligand.sdf` 开始，查看结构准备状态，再继续到后续对接流程。

如果当前 Python、RDKit 或 Meeko 不可用，可以使用 `prepared/` 下的参考 PDBQT 文件继续体验流程。

示例只用于学习 DockStart 操作流程，不用于药效判断或科研结论。
