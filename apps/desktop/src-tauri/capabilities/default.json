{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "DockStart 默认权限：核心能力 + 原生文件/目录选择对话框（tauri-plugin-dialog）。",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
