# DockStart 全功能扩展计划书

> 目标：在不牺牲 DockStart 现有“现代化、简洁、引导清晰、开箱即用”体验的前提下，逐步补齐 AutoDock Vina 已有但 DockStart 尚未完整支持的功能，并建立可持续扩展的对接协议架构。

- 项目仓库：<https://github.com/xuxinxi14/DockStart>
- 当前本地打包标识：DockStart v0.13.2；项目尚无任何公开 Release
- 当前科学开发状态：科学主链与证据收口完成，进入 v0.13.2 GUI 人工体验阶段
- 当前内置核心：AutoDock Vina 1.2.7
- 计划性质：产品路线图 + 技术架构方案 + 开发实施计划 + 验收标准
- 实施原则：分阶段实现和独立验收，不进行一次性大重写；先完成科学闭环，再进行实际体验与 UI 优化，最后才重新打包并决定首个公开 Release

---

## 1. 项目背景

DockStart 当前已经形成较完整的标准分子对接流程：

1. 创建或打开项目；
2. 导入或准备受体、配体；
3. 设置对接 Box；
4. 设置 Vina/Vinardo 常用参数；
5. 运行前检查；
6. 执行 AutoDock Vina；
7. 查看构象、分数和 RMSD；
8. 导出 CSV 与 Markdown 实验记录。

现阶段 DockStart 的主要优势不是修改对接算法，而是把分散的命令行工具、文件格式、参数设置和结果归档整理成清晰、可追踪、适合初学者的本地工作流。

下一阶段不应简单地把 AutoDock Vina 的全部命令行参数堆进一个“高级设置”页面，而应把不同科学用途拆分成明确的“对接协议”，为每种协议提供独立的输入要求、准备流程、参数、运行前检查、结果解析和报告模板。

---

## 2. 总体目标

### 2.1 产品目标

将 DockStart 从“标准刚性单配体对接工作台”逐步升级为：

> 面向 AutoDock Vina 生态的现代化、本地化、可追溯分子对接工作台。

完成后应覆盖：

- 标准刚性单配体对接；
- Vina 与 Vinardo；
- 高级搜索参数；
- `score_only` 现有姿势评分；
- `local_only` 局部优化；
- 批量虚拟筛选；
- 柔性侧链对接；
- 大环配体工作流；
- AutoDock4 affinity maps；
- AutoDock4 评分；
- 多配体共同对接；
- AD4Zn 专用协议；
- 水合对接；
- 外部 maps 导入、生成与复用；
- 完整任务队列、结果汇总和审计记录。

### 2.2 非目标

本计划不包含：

- 修改 AutoDock Vina 核心算法；
- 自研新的 scoring function；
- 将 docking score 描述为真实结合自由能；
- 自动生成药效、安全性或临床结论；
- 分子动力学模拟；
- QM/MM；
- 自动论文生成；
- 无人工确认的全自动受体修复；
- 无依据的自动质子化、电荷或结合位点判断。

### 2.3 成功标准

1. 初学者仍能在默认流程中完成标准对接，不被高级功能干扰。
2. 高级用户可以选择明确的协议，而不是手动拼装命令行参数。
3. 所有运行均保存输入、配置、命令、工具版本、日志、输出和哈希。
4. 每种协议都有独立的科学边界说明与失败提示。
5. Basic 安装包保持精简；Assisted 安装包继续开箱即用。
6. 复杂功能采用逐级披露，不破坏现有现代化界面。
7. 项目文件具有向前迁移、向后保护和异常恢复能力。

---

## 3. 产品设计原则

### 3.1 默认流程必须保持简单

默认首页和新建项目入口只突出三类常用场景：

- 标准小分子对接；
- 批量虚拟筛选；
- 现有姿势评分与优化。

柔性侧链、AutoDock4、AD4Zn、水合对接等高级协议放入“更多协议”，不在首页同时展示大量入口。

默认标准对接仍只要求用户理解：

- 受体；
- 配体；
- Box；
- 搜索彻底程度；
- 构象数量；
- 开始运行。

### 3.2 使用“协议”而不是“参数堆叠”

不建议继续把全部功能加入现有 `VinaSettings`。建议建立协议类型：

```text
rigid_single
batch_screening
score_only
local_only
flexible_sidechain
macrocycle
ad4_maps
multiple_ligands
ad4zn
hydrated_ad4_experimental
```

每种协议拥有：

- 独立输入模型；
- 独立工具依赖；
- 独立准备流程；
- 独立参数模型；
- 独立前置检查；
- 独立命令生成器；
- 独立输出解析器；
- 独立报告章节；
- 独立科学风险提示。

### 3.3 逐级披露

每个协议的参数页面分成三层：

#### 基础参数

只显示多数用户必须设置的内容。

#### 推荐设置

提供可解释预设：

- 快速预览；
- 标准；
- 深度搜索；
- 自定义。

#### 专家参数

折叠显示：

- `min_rmsd`；
- `max_evals`；
- `spacing`；
- `verbosity`；
- `no_refine`；
- `unbound_energy`；
- scoring weights 等。

专家参数必须显示默认值、单位、作用、风险、可比性影响，并提供“一键恢复默认值”。

### 3.4 自动化只能代替机械步骤，不能代替科学判断

允许自动完成：

- 路径管理；
- 文件复制；
- 配置生成；
- 工具检测；
- 格式检查；
- 任务排队；
- 日志归档；
- 结果汇总；
- 哈希记录。

必须要求人工确认：

- 受体链选择；
- 缺失残基处理；
- 水分子、金属、辅因子保留；
- 质子化状态；
- 配体形式电荷；
- 互变异构体；
- 柔性残基选择；
- 大环断环位置；
- Zn 配位环境；
- Box 是否覆盖真实研究位点。

### 3.5 保持本地优先

除主动下载 RCSB/PubChem 数据外：

- 结构准备在本机完成；
- 对接在本机完成；
- 项目数据在本机保存；
- 诊断在本机生成；
- 不上传私有结构；
- 不依赖云端账户才能运行。

---

## 4. 总体架构方案

### 4.1 协议注册中心

新增统一协议接口：

```python
class DockingProtocol:
    protocol_id: str
    display_name: str
    maturity: str
    required_tools: list[str]

    def validate_inputs(...)
    def prepare_inputs(...)
    def build_config(...)
    def build_command(...)
    def preflight(...)
    def parse_outputs(...)
    def build_report(...)
```

协议注册中心负责：

- 返回当前可用协议；
- 判断 Basic/Assisted 是否满足依赖；
- 返回协议说明；
- 返回表单 schema；
- 调用对应运行适配器；
- 统一保存审计信息。

### 4.2 项目数据模型升级

建议将当前项目 schema 升级为 v2：

```json
{
  "schema_version": 2,
  "project_name": "...",
  "protocol": {
    "id": "rigid_single",
    "version": 1,
    "maturity": "stable"
  },
  "inputs": {},
  "preparation": {},
  "search_space": {},
  "parameters": {},
  "runs": []
}
```

要求：

- 保留 v1 项目迁移；
- 旧项目默认映射为 `rigid_single`；
- 迁移前自动备份；
- 未知字段继续保留；
- 高版本项目不得被低版本程序强行改写；
- 每次运行保存协议版本，避免以后逻辑变化影响历史重现。

### 4.3 统一运行任务模型

所有协议使用统一状态：

```text
created
preparing
queued
running
cancelling
cancelled
finished
failed
interrupted
recoverable
```

任务记录至少包含：

- task_id / run_id；
- protocol_id；
- 输入快照；
- 配置快照；
- 命令数组；
- 工具版本与哈希；
- 开始、结束时间；
- PID 与进程身份；
- stdout/stderr/log；
- 输出文件；
- 解析结果；
- 错误代码；
- 重试来源；
- 取消原因；
- 恢复状态。

### 4.4 统一结果模型

```text
ligand_id
pose_id
affinity
rmsd_lb
rmsd_ub
status
warning_count
output_file
protocol
scoring_function
```

协议可附加扩展字段，例如柔性残基、大环断环键、maps 信息、Zn 配位参数、水合状态和多配体成员。

---

## 5. 全过程实施路线

# 阶段 0：架构准备与基线冻结

## 目标

先建立协议化、任务化和可迁移的数据基础，避免后续反复重构。

## 工作内容

### 0.1 冻结稳定基线

- 标记当前稳定版本；
- 保存 Basic 与 Assisted 安装态验收结果；
- 固定标准单配体基准案例；
- 固定 Vina 与 Vinardo 回归输出容差；
- 记录当前项目 schema；
- 建立升级前兼容测试。

### 0.2 引入协议抽象

- 建立 `DockingProtocol` 接口；
- 将现有标准对接迁移为 `rigid_single`；
- 保持现有前端流程和用户体验基本不变；
- 本阶段不增加新科学功能。

### 0.3 升级项目 schema

- 设计 schema v2；
- 实现 v1 → v2 迁移；
- 增加迁移备份；
- 添加协议版本；
- 添加运行模式字段；
- 添加扩展参数容器；
- 增加损坏项目、未知字段和高版本项目测试。

### 0.4 统一错误系统

错误代码按领域分组：

```text
PROJECT_*
INPUT_*
PREPARATION_*
PROTOCOL_*
MAPS_*
AUTOGRID_*
VINA_*
BATCH_*
FLEX_*
MACROCYCLE_*
AD4ZN_*
HYDRATED_*
RESULT_*
REPORT_*
```

错误提示统一包含：用户可读说明、原始错误、建议操作、影响范围、是否阻塞和可跳转页面。

## UI 要求

仅增加：

- 项目协议标记；
- 旧项目迁移提示；
- 兼容性说明；
- “查看迁移备份”入口。

## 验收标准

- 旧项目可以无损打开；
- 旧运行记录保持可查看；
- 标准对接结果与升级前一致；
- Basic/Assisted 均通过安装态测试；
- 未知字段不丢失；
- 迁移失败时原项目不被改写；
- 所有测试、构建和 `cargo check` 通过。

---

# 阶段 1：高级参数与轻量运行模式

## 目标

优先加入实现成本较低、科研用途明确的 Vina 原生能力。

当前状态：**姿势评价、局部优化定量比较与首批高级参数源码闭环完成，尚未发布**。`score_only` 已形成单点评分闭环；新准备的 `local_only` 已形成输入基线评分、局部优化、评分差、几何位移、同场 3D 叠合和报告闭环；`max_evals`、`min_rmsd`、`spacing`、`verbosity`、`no_refine`、`force_even_voxels` 与 `unbound_energy` 已接入单次运行，串行批量全局对接也继承其中除 `unbound_energy` 外的六项适用参数。`randomize_only` 仍未实现。本轮不修改 DockStart 版本号，也不改变任何既有安装包或发布状态。

## 功能范围

### 1.1 高级 Vina 参数

新增：

- `min_rmsd`；
- `max_evals`；
- `spacing`；
- `verbosity`。

- `no_refine`；
- `force_even_voxels`；
- `unbound_energy`。

默认行为与 Vina 保持一致；`unbound_energy` 未显式设置时保持 `None` 并省略配置项，不把 Vina 内部默认行为伪装成数值。

当前已接入：

- `max_evals` 和 `min_rmsd` 仅用于全局搜索；评价模式不写入无效参数；
- `spacing` 用于现场计算的 Vina/Vinardo 网格，AutoDock4 maps 下不写入；
- GUI 开放标准与详细两档日志；`local_only + verbosity = 2` 按明确边界解析优化前后能量块；
- `no_refine` 和 `force_even_voxels` 默认关闭，只在当前 Vina 的 `--help_advanced` 精确声明且达到最低安全版本时可启用；准备与执行阶段都会复核；
- `no_refine` 改用网格完成最终优化和评分，结果只与同设置运行比较；`force_even_voxels` 的取整会进入实际网格点数与资源估算；
- `unbound_energy` 默认留空，只在 Vina/Vinardo、刚性单配体 `score_only` 中写入；显式 `0` 与留空不同，只接受有限数值。最低门槛按 SemVer 与稳定版 Vina 1.2.4 比较，因此 `1.2.4-rc1` 不满足门禁；
- 对新生成的 `score_only` run，结果分析先核对日志 SHA256；显式参考还要求第 (1)～(4) 项编号和数值有效、第 (4) 项与冻结值一致，并验证总评分为 `(1) + (2) + (3) - (4)`。生成后的 `evaluation.json` 也以 SHA256 绑定到 run，读取和报告前都会复核。全局对接、`local_only`、柔性受体、AutoDock4 maps 和批量筛选均不使用该项；
- Box、spacing 与可移动原子类型联合估算网格内存，超出资源保护上限时在启动前阻断；
- 项目、配置、run 快照、批量队列快照、逐配体 config、报告、归档协议指纹、旧项目默认值和旧基础参数调用兼容均已覆盖。

### 1.2 `score_only`

以独立运行模式评价现有受体—配体姿势，不进行全局搜索，也不生成新构象。

本轮已接入：

- 保存 `run_mode = score_only` 的项目选择和不可变运行快照；
- 用户必须在同场 3D 视图复核输入姿势；确认记录绑定本次运行实际使用的刚性受体、可选柔性侧链与配体 SHA256，任一输入替换或柔性受体重建后失效，未确认时后端拒绝准备新 run；
- Vina/Vinardo 可使用项目 Box，也可显式启用 autobox；
- AutoDock4 maps 运行时由 maps 网格定义范围，autobox 必须关闭；
- 解析 Vina 能量分项，写入独立的 `evaluation.json`；
- 结果页显示姿势评分摘要与能量分项，不生成虚假的 pose 排名、Mode 1 或 RMSD；
- Viewer 查看的是本次运行冻结的输入配体快照；
- 输出独立的姿势评价 Markdown 报告。

未纳入本轮：自定义评分权重等其余专家参数。

### 1.3 `local_only`

以独立运行模式从输入姿势附近执行局部优化。它只探索输入姿势邻域，不等同于全局对接，也不能解释为已搜索整个 Box。

本轮已接入：

- 输入姿势预览；
- 普通 Box 或可选 autobox；AutoDock4 maps 下同样关闭 autobox；
- 同一不可变输入与配置下依次执行 `score_only` 基线和 `local_only`，分别保存阶段命令、时间、退出码、stdout、stderr、log 与 SHA256；
- 输出并查看 `optimized.pdbqt`；
- 解析输入与优化后能量分项，固定按“优化后－输入”计算评分差，写入独立的 `evaluation.json`；
- 按 Meeko SMILES 原子索引或严格 PDBQT 原子身份匹配，计算同一受体坐标系内不做对齐的重原子 RMSD、平均/最大位移与质心位移；
- 现代双阶段 run 的结果页默认在同一 3D 场景叠合冻结的受体、输入姿势与优化后姿势，并可单独显示任一姿势；叠合不做额外对齐，Viewer 读取前复核关键 SHA256；
- 基线失败或取消后不启动局部优化；日志或输出哈希变化时拒绝发布比较；
- 旧单阶段 `local_only` run 保持可读，缺少输入基线时明确显示不可比较，不填零。

未纳入本轮：轨迹动画、对称性重映射，以及把位移指标解释成共晶验证 RMSD。

### 1.4 `randomize_only`

作为专家工具，不作为主要首页入口。明确提示随机化姿势不是 docking 结果。

Vina 1.2.7 实测中 `randomize_only` 不遵守 CLI `--seed`，相同 seed 不能重放输出；同时它不产生评分或局部优化结果。因此它不作为高级参数之后的默认下一批能力，除非上游可复现性改变，或产品明确接受“不可复现的受限专家工具”定位。

## UI 设计

当前源码在统一的 Vina 运行工作台提供运行任务切换：

- 全局对接；
- 只评分；
- 局部优化。

切换只评分或局部优化后，界面隐藏仅适用于全局搜索的参数，并显示 autobox 选项和对应科学边界。帮助页、新建项目页和项目总览已经提供独立任务入口；文件来源仍按已有 PDBQT、原始结构转换和 Demo 单独选择。评价模式不开放独立 PubChem 在线构象入口，还要求用户在同场 3D 视图确认当前配体姿势，确认记录与本次运行实际使用的刚性受体、可选柔性侧链及配体 PDBQT 的 SHA256 绑定。标准对接参数页保持原有默认行为；`randomize_only` 仍不作为主要入口。

## 验收标准

- 默认标准流程不增加必填项；
- `score_only` / `local_only` 缺少当前输入姿势确认、确认哈希与现有文件不一致或任一输入不可读时，配置和 run 准备都必须阻断；
- prepared 评价 run 在执行前必须再次核对冻结确认与本次刚性受体、可选柔性侧链及配体输入快照；缺失、格式错误或 SHA256 不一致时不得启动 Vina；
- 新参数正确写入运行快照；
- 需要门禁的选项能力未知、不满足最低版本或未被高级帮助声明时必须在启动 Vina 前阻断；其中 `autobox` 要求稳定版 Vina 1.2.3 或更高版本；
- 准备后替换为不兼容的 Vina 可执行文件时，执行阶段必须再次阻断；
- 新生成的 `score_only` 日志缺少可信 SHA256、文件变化或哈希不一致时，不生成姿势评分结果；
- 显式 `unbound_energy` 只有在第 (1)～(4) 项完整、编号正确、第 (4) 项与快照一致且总评分满足 `(1) + (2) + (3) - (4)` 时，才记录为显式参考评分；
- `evaluation.json` 缺少可信 SHA256、内容变化或显式参考值类型非法时，结果页与报告均拒绝读取；
- `score_only` 不生成虚假的 pose 排名表；
- `score_only` 成功不依赖 `out.pdbqt`，并保留不可变输入姿势用于查看；
- `local_only` 正确保存优化后结构；
- 新 `local_only` 必须按顺序完成输入评分与局部优化，两阶段使用同一冻结输入、配置与评分协议；
- 差值定义、原子映射方法和“未对齐”边界在结果页与报告中明确可见；
- 任一阶段失败、取消、工具二进制变化或关键日志篡改时不生成可比较结论；
- 报告明确区分全局对接、评分和局部优化；
- 不同 scoring function 的分值不横向混排。

兼容性与发布边界：

- 当前仍使用 schema v1 的兼容扩展字段 `docking_protocol.run_mode` 与 `docking_protocol.autobox`，没有为了本功能迁移或重写用户项目；
- 旧项目缺少这些字段时固定解释为 `run_mode = dock`、`autobox = false`，继续执行原有全局对接；
- 已完成的历史评价 run 不因缺少新确认字段而停止读取；旧评价项目再次创建 run 时必须先对当前输入完成哈希绑定确认，旧的未执行 prepared 评价 run 缺少该记录时必须重新准备；
- 旧项目缺少七项高级参数时恢复各项默认行为；`unbound_energy` 缺失时保持空值并不写入配置。旧调用方只提交基础参数时不覆盖已经保存的高级值；
- 批量筛选仍固定使用全局对接，不继承单次运行的姿势评价模式；
- 批量筛选继承适用于全局对接的六项高级参数，并冻结到队列、逐配体 config、整批报告和归档协议指纹；两个专家开关继续执行当前 Vina 能力门禁及执行前复核，`unbound_energy` 不进入批量；
- 本轮已通过源码树内置 Vina 1.2.7 的真实双阶段回归、两个专家开关 smoke 与显式 `unbound_energy` 评分闭环，并覆盖基线失败、阶段间取消、现代 `score_only` 日志 SHA256 篡改、未结合态项号/快照/能量平衡不一致、严格原子映射、能力门禁和旧 run 兼容；这不代表 Basic/Assisted 安装包已经包含或验证该能力；
- 发布前仍需完成 Basic/Assisted 安装态回归、用户文档截图、安装升级路径和发布材料复核。

---

# 阶段 2：批量虚拟筛选

## 目标

建立 DockStart 最重要的扩展能力：单受体、多配体的本地批量筛选工作流。

## 功能范围

### 2.0 当前工作树实现状态

已完成的闭环包括：多文件或目录递归导入 PDBQT/SDF/MOL、多分子 SDF 逐记录准备与导入预览、固定受体/Box/基础参数与六项适用高级参数快照、稳定串行队列、逐项独立 config 与 attempt、失败重试、安全取消与恢复、归档、全量 CSV、Top N、完整结果检索/筛选/排序/分页、逐配体最佳构象查看、批量 Markdown 实验记录、归档列表与只读历史结果浏览、双归档严格只读比较，以及单归档可校验 ZIP 导出。

逐配体 Viewer 使用本次筛选冻结的受体与该 item 成功 attempt 的输出；归档 Viewer 还按合法 `archive_id` 将读取范围限定在所选归档，只开放成功配体的 Mode 1。新任务保存输出 SHA256，哈希不一致时拒绝显示；旧归档没有输出哈希时保留可读，但标记为历史未完全验证。前端仅在用户点击某个成功项时加载其结构，不一次性加载整个配体库。

归档索引会保留损坏条目及错误原因，但在归档标识、清单、状态哈希或目录边界校验失败时阻止详情和 3D 读取。归档保持只读，不能恢复为活动队列，也不提供编辑、重试或取消。

双归档比较要求恰好选择两个不同且有效的归档，选择顺序固定为基线和对照。后端先实际核对两批冻结受体及全部冻结配体输入的文件大小与 SHA256，再仅按 ligand 输入 SHA256 建立逐项匹配；重复 SHA256 标记为 `ambiguous`。协议指纹覆盖受体 SHA256、评分函数、Box 六项、Vina 版本与二进制 SHA256，`exhaustiveness`、`num_modes`、`energy_range`、`cpu`、`seed`，以及 `max_evals`、`min_rmsd`、`spacing`、`verbosity`、`no_refine`、`force_even_voxels`。只有协议相同、身份唯一、两侧成功且评分与排名有效时，才计算 `Δ = 对照－基线`；协议不一致仍并排显示但不计算差值。整个比较过程只读，不修改 `project.json` 或归档内容。

单归档 ZIP 导出只接受安全可读的历史归档，并以 allowlist 形成 `payload/<archive_id>/`。根清单 `dockstart_screening_export.json` 记录精确成员、逐文件大小与 SHA256、payload 树哈希、源完整性状态及兼容警告；写入后重新执行 ZIP CRC、成员集合和逐文件哈希验证。现代归档会把 `attempt.json` 与冻结 attempt 记录绑定，并为构象、完整汇总、Top N 和 Markdown 实验记录保存及核对历史大小与 SHA256；旧归档缺少这些凭据时允许保留部分验证状态导出，但不会用导出时的新哈希升级成现代完整验证。GUI 始终按“不覆盖”发布；目标已存在时保留原文件并要求另选名称，避免保存对话框与实际写入之间的覆盖竞态。包不包含 Vina 二进制、活动队列、项目当前状态或 staging，不能直接导入或恢复运行；已有审计 JSON 可能含绝对本机路径，因此默认不匿名。清单哈希验证导出包字节，不是数字签名或历史真实性证明。

配体库导入现在先在启动 RDKit/Meeko 前盘点容器大小、逻辑记录数和单记录大小。SDF 中每条记录保持 1-based 原始位置，单条读取或准备失败只进入导入预览，不会吞掉其他有效分子。每条 SDF/MOL record 的精确原始字节会冻结到项目内不可变目录；隔离 worker 从该拓扑计算形式电荷、重原子数、严格可旋转键、片段、最大环与 canonical graph 身份。失败项可以只从冻结 record 发起审计重试，并保存独立 JSON/CSV 失败清单；七元及以上大环进入不可自动重试的 `review_required`。准备后的 PDBQT 与 canonical topology 共同组成现代逻辑身份，创建队列时会交叉核对 staging 文件、revision 和拓扑证据，并把 record 级来源冻结到 `screening.json` 与后续 `attempt.json`。成功且具有可信拓扑的结果还能通过真实 Meeko 映射生成聚合 SDF，并由隔离 RDKit 逐构象验证规范重原子图。该增量不迁移 `project.json` 或 `screening.json` schema，也不代表已经公开发布。

当前未完成的产品增强包括：并行队列、单项强制取消、PDBQT 打包、归档删除、ZIP 导入/恢复与多个构象并排查看。后续规划不得再把已经完成的原始 record 冻结、化学事实、失败重试/清单、拓扑验证 SDF 或可校验归档 ZIP 写成从零开发。

### 2.1 配体库导入

当前源码已支持：

- 选择多个 PDBQT；
- 递归导入目录中的 PDBQT/SDF/MOL，并跳过链接与非支持格式；
- 导入 SDF 多分子文件；
- SDF/MOL 逐记录准备，单条失败隔离；
- 将每条 SDF/MOL record 的精确字节冻结为项目内不可变拓扑输入；
- 显示形式电荷、重原子数、严格可旋转键、片段和最大环等可核对事实；
- 从冻结 record 对失败准备项执行审计重试，并导出 JSON/CSV 失败清单；
- 按准备后 PDBQT 与 canonical topology 的复合身份识别现代重复内容；
- 生成基于源文件内容和 1-based record index 的稳定候选 ID；
- 保存源文件、record index、record SHA256、revision、准备证据与准备后文件映射；
- 在建队前显示可用、重复、失败和已选记录，只有勾选的唯一可用项进入队列。

导入表的进一步搜索、过滤和大规模虚拟列表仍可作为后续交互增强，但不属于当前科学闭环缺口。

### 2.2 配体表格

当前导入预览已显示：

- 名称；
- 稳定候选 ID（内部使用）；
- 来源；
- 准备状态；
- 错误状态；
- 是否加入运行；

队列结果工作区另行显示最新结果并支持搜索、过滤、排序和分页。导入预览已经显示来源、准备状态、化学事实、错误状态和选择状态，并提供基于冻结 record 的失败项审计重试及失败清单；导入表自身的进一步搜索过滤属于后续交互增强。

### 2.3 任务队列

当前已实现：

- 队列生成；
- 顺序执行；
- CPU 总量上限；
- 队列级安全取消：取消请求在当前配体安全结束后生效，不强制终止正在写结果的 Vina 进程；
- 中断恢复；
- 失败重试；
- 跳过已成功任务；
- 防止重复输出覆盖。

当前执行器固定为 DockStart 自己的串行队列，逐个调用 Vina，以保留精细进度、独立 attempt、单项重试、独立日志和恢复。100 项真实资源基准支持继续采用该模型；并行执行、正在运行项的强制取消或原生 Vina batch 模式只有在出现明确吞吐需求，并由可选 500 项容量 probe 与写入安全设计支持后才另行立项。

### 2.4 结果汇总

当前源码已支持：

- Top N；
- 每个配体最佳 affinity；
- 每个配体最佳 pose；
- 状态筛选；
- 错误筛选；
- CSV 导出；
- 按分数排序；
- 点击配体打开 3D pose；
- 文本检索、四种排序和分页；
- 归档列表、只读结果与报告状态；
- 按 `archive_id` 安全查看历史成功配体的 Mode 1。
- 恰好两个有效归档的严格只读比较，现代记录按冻结 PDBQT SHA256 与 canonical topology SHA256 的复合身份匹配，并明确显示协议不一致与歧义项；
- 有可信冻结拓扑时，通过真实 Meeko 映射生成聚合 SDF，并由隔离 RDKit 逐构象验证 canonical heavy-atom graph；
- 将安全可读的单个历史归档导出为带根清单、逐文件大小/SHA256、payload 树哈希与写后 CRC/成员/哈希复核的 ZIP。

仍待实现：

- PDBQT 打包；
- 归档删除；
- ZIP 导入/恢复；
- 选择多个结果并排比较。

### 2.5 批量报告

当前终态任务可生成 `screening_report.md`，包含配体总数、成功/失败/未完成数量、评分函数、Box、基础参数、六项适用高级参数、Vina 版本与哈希、Top N、全量结果、单项错误和科学免责声明。运行耗时统计与失败原因聚合图仍可后续增强。

## UI 设计

当前采用同一对接工作台中的两个明确状态：创建前显示配体库与队列参数；创建后显示队列控制、指标、输出文件和完整结果工作区。单配体和多配体仍由导入数量自动分流，避免把批量参数塞入单配体界面。多配体模式继承当前全局对接设置中的 `max_evals`、`min_rmsd`、`spacing`、`verbosity`、`no_refine` 和 `force_even_voxels`，创建前明确展示即将冻结的值；`unbound_energy` 不适用于批量队列。多配体面板使用“当前任务 / 历史归档”入口分离可操作队列与只读历史，不在归档详情或双归档比较中显示恢复、编辑或重试操作。比较入口必须明确显示先选批次为基线、后选批次为对照，并在协议不一致时保留并排记录而隐藏无效差值。

## 验收标准

- 项目当前硬上限为 500 个配体；已完成 50 个真实 Vina 子进程压力测试，后续应以测得的内存和 GUI 响应为依据增加 100/500 项门禁；
- 单个配体失败不阻塞整个队列；
- 中断后可识别已完成项；
- 不覆盖历史结果；
- 总 CPU 不超过用户上限；
- 每个配体有独立输入、配置、日志和结果；
- 每个配体的 config 必须使用队列冻结的六项适用高级参数；`no_refine` 与 `force_even_voxels` 必须通过当前 Vina 能力门禁，并在开始或恢复队列时统一复核；
- 每次 attempt 必须把冻结受体与当前配体按实际字节写入独立目录，在 Vina 启动前和返回后分别核对受体、配体、配置与当前 Vina 二进制的大小和 SHA256；全部一致才允许成功，`attempt.json` 必须记录本次实际使用的输入、配置与 Vina 快照证据；
- 新队列必须冻结并哈希全部九项 `resource_limits`；创建、开始、恢复、归档详情与比较必须共用严格校验，覆盖重试次数、Top N、配体数量、单配体与总输入大小及全部计算边界，且任何限制都不能放宽超过应用硬上限；
- Top N 与原始结果一致；
- 大型项目不一次性把全部结构内容载入前端内存。
- 逐配体 Viewer 不能越出当前 item 的 attempt 目录读取输出，输出哈希变化必须使缓存失效并阻止误展示；
- 历史 Viewer 必须按 `archive_id` 限定归档目录；损坏归档保留可见但阻止详情，旧归档缺输出哈希时显示未完全验证警告；
- 归档详情保持只读，不能恢复为活动队列或修改历史参数；
- 双归档比较必须先核对两个归档的受体与全部配体输入 size/SHA256；只能按冻结 ligand 输入 SHA256 匹配，重复身份必须标为 `ambiguous`；
- 协议指纹必须包含 `max_evals`、`min_rmsd`、`spacing`、`verbosity`、`no_refine` 和 `force_even_voxels`；仅当协议指纹相同、身份唯一、两侧成功且评分与排名有效时计算“对照－基线”差值；协议不同只并排显示，且比较不能写入项目或归档；
- 批量报告必须从当前终态筛选快照生成并列出六项适用高级参数，不能读取后来替换的项目参数。

---

# 阶段 3：柔性侧链对接

## 目标

支持受体有限侧链柔性，同时保持操作可理解、可追踪。

## 功能范围

### 3.1 柔性残基选择

- 在 3D 视图点击残基；
- 按链与残基号搜索；
- 显示距参考配体或 Box 中心的距离；
- 限制选择数量；
- 排除不完整残基；
- 明确显示当前选中残基。

### 3.2 rigid/flex 受体准备

- 生成 rigid PDBQT；
- 生成 flex PDBQT；
- 保存链、残基号、残基名称；
- 保存准备命令；
- 验证 rigid/flex 文件一致性；
- 检查重复原子和缺失原子；
- 记录准备工具版本。

### 3.3 参数与运行

增加柔性残基数量、搜索彻底程度建议、CPU 与运行时间提示、Box 覆盖检查和自由度风险提示。

### 3.4 结果显示

3D Viewer 支持：

- 刚性受体；
- 柔性侧链；
- 配体 pose；
- 柔性侧链前后构象比较；
- 按残基高亮；
- 结果中的柔性残基列表。

## UI 设计

采用向导：导入结构 → 检查结构 → 选择柔性残基 → 生成 rigid/flex → 设置 Box → 设置参数 → 运行。

## 验收标准

- 官方柔性对接案例可复现；
- rigid 与 flex 原子划分无重复；
- 不完整残基被阻止或明确警告；
- 柔性残基写入报告；
- 普通刚性项目不受影响；
- 结果页面能区分配体变化和侧链变化。

---

# 阶段 4：AutoDock4 maps 基础设施

## 目标

建立 AD4、AD4Zn 和水合对接共同依赖的 maps 基础设施。

## 功能范围

### 4.1 AutoGrid4 工具链

- 完成许可证与分发审查；
- 确定 Basic/Assisted 分发策略；
- 工具检测；
- 版本记录；
- 哈希校验；
- 安装态验收。

### 4.2 GPF 生成

支持受体、原子类型、grid center、grid points、spacing、参数文件、maps 前缀、AutoGrid 日志和错误解析。

### 4.3 maps 管理

- 生成 maps；
- 导入已有 maps；
- 验证 maps 完整性；
- 识别缺失原子类型；
- 保存 maps manifest；
- 记录 maps 与受体的哈希关联；
- 复用 maps；
- 受体变化后旧 maps 自动失效。

### 4.4 AutoDock4 评分协议

新增独立协议 `AutoDock4 (maps)`：

- 不与 Vina/Vinardo 放在普通评分函数下拉框中；
- 必须先准备或导入 maps；
- 分数结果独立展示；
- 报告明确注明 AD4；
- 禁止把 AD4 分数与 Vina/Vinardo 直接比较。

## 验收标准

- 官方 AD4 案例可复现；
- maps 缺失时禁止运行；
- 原子类型不匹配时给出明确说明；
- 受体变更后旧 maps 自动失效；
- maps manifest 完整；
- AutoGrid 错误不会被误报为 Vina 错误。

## 历史 v0.12.0 源码/打包基线实施结果

阶段 4 已完成并曾进入打包验证，但项目尚未公开发布 Release：

- AutoGrid4 已按外部 GPL 工具处理，Basic/Assisted 均不内置；设置页、工具检测页和 maps 面板可识别用户配置或 PATH 中的 `autogrid4.exe`。
- GPF、maps 生成/导入、manifest、网格与原子类型校验、受体/文件 SHA256 绑定、复用和自动失效已经落地。
- AutoDock4 maps 作为独立协议接入配置、运行前检查、不可变 run 快照、Vina 1.2.7 执行、结果解析和 Markdown 报告。
- AD4 项目级 scores/report 使用独立文件名，GUI 和报告禁止与 Vina/Vinardo 直接横向比较。
- 官方 `1dwd` 非金属用例已穿过 DockStart 完整工作流，生成 `ad4_001` 与 `run_001`，最佳评分 -11.55 kcal/mol。

该历史基线不含阶段 6 的多配体共同对接、阶段 7 的 AD4Zn 或阶段 8 的水合 AD4，也不开放柔性受体 AD4 或批量 AD4 maps。阶段 6～8 后来都已在当前工作树中形成彼此隔离的实验性闭环，但不能反向写入旧安装包。按 2026-07-30 当前源码重新校准后的路径为：

```text
历史 v0.12.0 AutoDock4 maps 打包基线（未公开 Release）
→ 大环、有限柔性与串行批量的真实工具链复核
→ 阶段 7 AD4Zn beta 源码闭环
→ 阶段 6 多配体共同对接实验性源码闭环
→ 阶段 8 水合 AD4 Experimental 源码闭环
→ 补齐可重放科学证据、正向水合基准与规模基准
→ 实际体验和 UI 优化
→ 最后决定首个公开 Release 的版本号与 Stable 协议集合
```

---

# 阶段 5：大环配体正式支持

## 目标

把 Meeko 可能已有的底层大环准备能力升级为正式、可审计的 DockStart 协议。

## 功能范围

### 5.1 大环检测

- 检测环尺寸；
- 检测可处理的大环；
- 标记潜在断环键；
- 显示 Meeko 支持状态；
- 不支持时给出明确原因。

### 5.2 断环选择

- 自动建议；
- 3D 显示候选键；
- 人工确认；
- 保存最终断环位置；
- 记录原子编号与键类型；
- 恢复默认建议。

### 5.3 PDBQT 准备验证

检查 glue atom、断环标记、环原子对应关系、输出完整性、准备日志和 Meeko 版本。

### 5.4 结果解释

报告明确说明断环与伪原子方法、环闭合检查要求、分数解释限制和断环位置影响。

## UI 设计

普通配体准备检测到大环时，显示“检测到大环配体”与“进入大环准备”按钮，不直接失败，也不静默当作普通配体处理。

## 验收标准

- 官方大环案例可复现；
- 断环位置完整记录；
- glue atom 可验证；
- 不支持的大环不会被静默接受；
- 普通非大环流程不增加额外步骤。

---

# 阶段 6：多配体共同对接

## 目标

支持两个已准备配体在同一次 Vina 搜索中共同参与对接，并把联合构象与联合评分安全地呈现给用户。

## 当前状态

**“多配体共同对接（实验性）”已在当前工作树形成源码闭环；本轮不修改版本号、不重新打包，项目也尚未公开发布 Release。**

它是独立协议，不是当前串行批量筛选的别名。导入多个配体仍默认进入“多个配体分别运行”的串行批量流程；只有用户显式选择本实验协议时，两个成员才会进入同一次搜索。

## 功能范围

- 一个受体；
- 恰好两个已经准备好的 PDBQT 配体，按用户确认的顺序冻结成员关系；
- 刚性受体、显式 Box、全局对接；
- Vina 或 Vinardo 评分，AutoDock Vina 最低版本 1.2.0；
- Vina 命令使用一个 `--ligand` 参数，后面依次传入两个成员路径，而不是分别启动两个任务；
- 显示各配体原子数与可旋转键；
- 计算总自由度；
- Box 覆盖检查；
- 统一运行；
- 多配体 pose 查看与分别着色；
- 保存成员关系；
- 输出中识别各配体；
- 报告说明其不是批量筛选。

首版明确排除：

- 柔性受体；
- AutoDock4、AD4Zn 或任何预计算 maps；
- `score_only`、`local_only`；
- 水合对接；
- 少于或多于两个成员。

本协议只复用现有 AutoDock Vina、PDBQT、项目记录和 Viewer 基础设施，不新增第三方依赖、外部工具或许可证义务。

## 结果语义

- 每个输出 `MODEL` 是两个成员组成的一组联合构象，不是可以拆成两个独立排名的单配体结果；
- 每个 Mode 只有一个联合评分。不得生成、推断或展示成员 affinity，也不得用两个单配体评分求和代替联合评分；
- 不直接比较成员组成或成员数量不同的联合任务；这种比较没有与单配体批筛相同的排名语义；
- stdout 的结果表可能包含因 `energy_range` 最终未写入输出 PDBQT 的行。构象可用性和成员读取必须以 `out.pdbqt` 中实际存在且带结果记录的 `MODEL` 为准，不能用 stdout 行数补造 pose。

## UI 设计

协议选择中明确区分：

- 串行批量筛选：多个配体分别运行，每个配体有独立任务与评分；
- 多配体共同对接（实验性）：恰好两个配体在同一搜索中运行，每个 Mode 只有联合评分。

共同对接工作区必须显示成员顺序、刚性受体/全局搜索/Vina 或 Vinardo 边界、版本门槛和不支持组合。结果 Viewer 可分别着色、显示或隐藏两个成员，但不能在颜色图例旁放置虚构的成员分值。

## 验收标准

- AutoDock Vina 1.2.0 以下、帮助未声明多路径 `--ligand` 或版本不可确认时，在启动前阻断；
- 命令严格为一个 `--ligand` 后跟两个有序输入路径，并显式指定联合输出文件；
- 官方 5X72 两配体案例已于 2026-07-28 使用仓库随附 AutoDock Vina 1.2.7 完成源码级真实全链路；命令、成员顺序、双成员 Viewer、评分表和报告均通过，官方参考输出 SHA256 `9fd1901bb52d0c767674fdd6e926f749e9995a35aa5b51e0318fd9e6f6922764` 可解析为 7 个双成员 Mode，最佳联合评分 -19.043 kcal/mol；
- 结果中能区分每个配体；
- 输出不会被错误解析成单一配体；
- stdout 行数多于输出 `MODEL` 数时，界面只把实际写入 `out.pdbqt` 的 Mode 标记为可查看；
- 联合评分不会被拆成成员 affinity，不同成员组合或成员数不会显示直接比较结论；
- Box 覆盖不足时给出明确门禁或警告，自由度过高时给出成本提示；
- 覆盖输入/输出篡改、成员顺序变化、缺少成员、取消与失败恢复、历史项目兼容以及 Basic/Assisted 安装态 GUI 回归。

---

# 阶段 7：AD4Zn 专用协议

## 目标

支持 Zn 金属蛋白的专用 AD4Zn 对接流程。

## 当前状态

**AD4Zn beta 已进入当前源码工作树，但尚未修改版本号、重新打包或进入正式 Release。**

它不是新评分函数：DockStart 生成 TZ 受体，使用用户提供的 `AD4Zn.dat` 与 AutoGrid4 4.2.7+ 生成专用 maps，再由 Vina 以 `--maps <prefix> --scoring ad4` 运行。标准 AutoDock4 maps 基础设施继续独立存在，两种协议不能静默互换。

## 功能范围

### 7.1 Zn 环境识别

- 检测 Zn，并独立列出其他金属；
- 按 v1.2.7 `zinc_pseudo.py` 固定参考的距离键图、羧酸加权代表点和 1-2/1-3 排除列出附近 N/O/S 配位原子和残基；
- 忽略已有 TZ，并把用于协议的 Zn 电荷置零；
- 只有恰好三个受体配位方向且保留一个稳定的开放四面体方向时，才在距 Zn 约 2.0 Å 处生成一个 TZ；近共面位点直接要求人工处理；
- 目标 Zn 的 4.5 Å 邻域含其他金属时阻止自动生成；不把多核位点或 Mg、Fe、Ca 等其他金属自动当成 Zn 协议，未生成 TZ 时硬阻断，不降级到标准 AD4。

### 7.2 TZ 与参数

- 准备项目内独立 TZ 受体，保留原始受体不变；
- 用户从固定上游参考取得并选择 `AD4Zn.dat`；项目复制用户触发的副本，校验受支持关键参数与 GPL 声明，并记录本机来源路径、实际 SHA256、许可证 ID、支持配置和上游参考；
- 生成包含 AD4Zn 专用 `nbp_r_eps` 覆盖的 GPF；
- 仅允许 AutoGrid4 4.2.7 或更高版本，GLG 必须显示成功完成；
- 生成独立 `ad4zn_*` maps 资产并保存 manifest；
- 验证 TZ/Zn 几何、参数、GPF、全部 maps、工具版本和文件 SHA256。

### 7.3 专用报告

记录原始受体与 TZ 受体、Zn、配位残基、TZ、参数文件来源/许可证/hash、GPF、maps、Box、AutoGrid/Vina 证据、用户确认项和科学边界。scores 与报告使用 AD4Zn 专用文件名，不覆盖标准 AD4 或 Vina/Vinardo 结果。

## UI 设计

AD4 顶层协议中保留“标准 AD4 / AD4Zn beta”二级选择。AD4Zn 页面提供站点状态、TZ 几何、参数文件、AutoGrid 兼容性和显式确认清单；准备完成不等于 maps 就绪，只有整个 maps manifest 通过门禁后才允许运行。

用户必须确认目标 Zn、配位环境、质子化、水、辅因子、TZ 几何和“仅限 Zn”。确认绑定受体 SHA256 与站点，输入变化后自动失效。

## 许可证与分发边界

`AD4Zn.dat` 文件自身声明 GPL-2.0-or-later，不随 DockStart 仓库、Basic 或 Assisted 安装包分发，也不由应用静默下载。用户从 AutoDock Vina v1.2.7 固定上游参考自行取得并选择；项目会为可复现性保存副本，并记录本机来源路径、实际 SHA256、许可证 ID、受支持配置和上游参考。分享含该副本的项目时由分享者履行 GPL 再分发义务。未来若提供应用内下载或离线组件包，必须先单独完成 GPL 源码提供、notice 与再分发方案审查。

## 验收标准

- 官方 1S63 TZ 坐标参考已经达到零误差；1S63、2OI0、1R1J 的真实 AutoGrid/Vina 全链已经完成，仍需更多不同单核 Zn 几何样本评估成熟度；
- 非 Zn 金属不会误进入协议；
- 缺失 AD4Zn 参数时阻止运行；
- AutoGrid4 低于 4.2.7、GLG 未成功完成、TZ 缺失或几何异常、参数/GPF/maps/hash 不一致时阻止运行；
- run 前后可以复核原始受体、TZ 受体、参数、GPF、maps 和工具证据；
- 报告不泛化到其他金属体系。

2026-07-30 已完成 1S63、2OI0、1R1J 真机链，以及无 TZ、多金属、非 Zn、参数篡改、工具失败和恢复门禁。当前科学剩余是扩大单核 Zn 几何样本并形成明确的适用/拒绝解释；安装态、历史项目和 GUI 属于后续体验与发布阶段。TZ 是几何伪原子，AD4Zn 分值不能与标准 AD4、Vina 或 Vinardo 直接比较，也不能替代实验验证。

---

# 阶段 8：水合对接

## 目标

提供独立标记为实验性的 hydrated docking 工作流。

## 当前状态

**`hydrated_ad4_experimental` 已在当前工作树中形成后端、Tauri 桌面桥接和 GUI Experimental 闭环；本轮不修改版本号、不重新打包，项目也尚未公开发布 Release。**

阶段 4 的标准 AD4 maps 基础设施、特殊 W 类型、clean-room W map、逐构象水分子后处理和固定 1UW6 外部验收已经存在。2026-07-30 又完成 2BYS/2ZJU 当前项目 API 的真实 AutoGrid/Vina 全链；当前重点已从“补项目级主链”转为“增加独立正向姿势恢复和更广泛科学基准”。

## 已实现功能范围

### 8.1 独立水合配体准备

- 只接受项目内单分子 SDF/MOL，要求一个有效三维 conformer、有限坐标和明确键级；
- 使用兼容 RDKit 与 Meeko `MoleculePreparation(hydrate=True)`，并先以真实小分子探针确认当前工具链可以生成 W 原子；
- 生成独立的加氢 SDF 和水合 PDBQT，不覆盖 raw 配体、标准 `prepared/ligand.pdbqt` 或当前标准协议；
- 每次准备写入 `protocols/hydrated/ligand_preparations/hydrated_ligand_NNN/`，保存源快照、独立脚本、命令、工具版本/路径/哈希、stdout/stderr、输出和 manifest；
- 准备、发布或激活期间输入、脚本、Python、RDKit 或 Meeko 变化时保留失败审计并拒绝激活。

### 8.2 水合 AD4 maps

- 只接受当前项目刚性受体 PDBQT、显式项目 Box 和已激活的水合配体；
- AutoGrid4 基础 GPF 不把 W 作为普通 ligand type 传给 AutoGrid；它为实际配体类型补齐 OA/HD 源 map；
- DockStart 以 clean-room 固定 BEST 规则生成 `receptor.W.map`：OA/HD 权重均为 `1.0`，任一源值大于 `0` 时写入 `-0.2 kcal/mol` 置换熵，否则取更有利源值乘以 `0.6`；
- 独立 `maps/hydrated_NNN/hydrated_manifest.json` 绑定受体、水合配体及其 manifest、Box、AutoGrid 证据、OA/HD/W 参数、全部 map 文件、网格几何和 SHA256；
- 受体、Box、水合配体、manifest 或任何 map 改变时阻断运行；已经完整发布的 maps 不要求生成工具之后仍安装在同一路径。

### 8.3 冻结运行与后处理状态

- 最低要求 AutoDock Vina 1.2.0，并要求当前运行时明确通过 `--maps` 能力门禁；
- 运行准备冻结刚性受体、水合配体、两个 manifest、全部 maps、配置和 Vina 二进制证据；
- Vina 命令固定使用 `--maps <prefix> --scoring ad4`，不传 `--receptor`，只执行全局 `dock`；
- Vina 成功后先保留 raw `runs/run_NNN/out.pdbqt`，再进入显式 `postprocessing` 阶段；
- 后处理生成 `hydrated_retained.pdbqt`、`ligand_water_free.pdbqt` 和 `waters_manifest.json`。失败时 run 以 `postprocess_failed` 终止并保留 raw 输出，不发布部分派生结果；
- CPU 内后处理阶段不接受取消；进程异常中断时保守收敛为失败，不伪装成可恢复成功。

### 8.4 水分子分类与结果语义

- 每个 Vina MODEL 独立处理，W 身份只按最终 PDBQT 原子类型判断，不按 atom name 猜测；
- W 与配体重原子或受体非 HD 原子距离严格小于 `2.03 Å` 时移除；
- 其余 W 在 `±1 Å` 邻域采样 W map 最小值，`< -0.5` 为强水，`< -0.3` 为弱水，其余为置换水；
- 保留水输出只保留强/弱 W 并加入 DockStart 注释，去水输出移除全部最终类型为 W 的原子；原始 MODEL 顺序、扭转树、非 W 原子和 `REMARK VINA RESULT` 保持不变；
- 结果页、`results/hydrated_ad4_scores.csv` 和水合 Markdown 报告显示 raw hydrated AD4 affinity、RMSD 和逐构象水统计；Viewer 默认读取保留水输出，SDF 导出只接受当前 run 已校验的去水文件；
- raw affinity 只用于同一 run 内 pose 排序，不用于虚拟筛选、跨配体、跨协议或处理前后比较。**处理后评分未计算**，当前不存在 dry affinity、校正 affinity 或重评分结果。

## 首版组合边界

只支持：

- 一个 SDF/MOL raw 配体；
- 一个刚性受体 PDBQT；
- 显式项目 Box；
- AD4 预计算 maps；
- 全局对接；
- Vina 1.2.0+；
- 外部 AutoGrid4 与兼容 RDKit/Meeko。

明确不支持：

- 柔性受体；
- 串行批量筛选或跨配体排名；
- 多配体共同对接；
- Vina/Vinardo 评分或预计算 maps；
- 标准 AD4、AD4Zn 与水合协议的静默互换；
- `score_only`、`local_only`、autobox；
- 大环 ghost atom、AD4Zn 或其他实验协议组合；
- 处理后重评分。

AutoGrid4 继续是用户自行安装/配置的 GPL 外部工具，不随 Basic 或 Assisted 分发。当前源码闭环没有新增随包 GPL 组件；水合配体准备仍依赖兼容 RDKit/Meeko，正式发布前必须明确 Basic、Assisted 与用户配置 Python 的实际支持矩阵。

## UI 设计

当前源码入口位于：

```text
打开项目 → 左侧工作台 → 水合 AD4
```

页面标题和协议范围卡始终显示 Experimental。当前页面已经按五个有依赖关系的步骤呈现：

1. 准备水合配体；
2. 生成并校验水合 AD4 maps；
3. 运行前检查；
4. 创建 run 并进入现有 Vina 执行页；
5. 读取水合结果。

页面持续显示 Experimental、单配体、刚性受体、全局对接、AD4 maps、不用于虚拟筛选和“处理后评分未计算”。耗时操作使用统一加载窗口；受体、raw 配体、Box、active manifests、maps、运行记录和阻塞原因在同页可见。通用结果页也识别水合协议，显示协议边界和逐 Mode 水统计。

## 源码验证状态

- 自动化测试覆盖准备输入与工具门禁、TOCTOU/篡改、BEST W map、AutoGrid hydrated profile、项目 maps 生成/失效、逐 MODEL 后处理、run 冻结/执行/失败、取消/恢复、结果/报告、Viewer/SDF 边界和桌面 API 参数映射；
- 结果证据严格绑定 stdout/log、verbosity 1/2 的评分表词法格式、Mode/affinity/RMSD 语义、`num_modes`、`energy_range` 造成的输出连续前缀、PDBQT 固定三位序列化以及已登记 scores.csv；未登记 CSV 不参与现代 run 结果解释；
- 外部 1UW6 fixture 只提交固定 tag/commit、文件大小、SHA256 和期望值，不复制上游结构/结果；
- `scripts/verify_hydrated_1uw6.py` 使用用户提供的 AutoDock Vina v1.2.7 固定 checkout，分别验证 Meeko 0.7.1 生成 2 个 W；clean-room BEST W map 的 68,921 个值与上游参考逐点一致（规范化序列化后的文件字节不同，不宣称 SHA256 相同）；随附 Vina 1.2.7 以 AD4 maps 运行得到最佳评分 `-8.261 kcal/mol`，请求最多 9 个 Mode 时实际输出 8 个；官方独立 9-pose raw 输出的 18 个 W 被分类为 8 强、1 弱、9 置换且 raw affinity 不变；
- `scripts/verify_hydrated_multisystem.py` 已用公开 2BYS/2ZJU、AutoGrid4 4.2.7 与 Vina 1.2.7 完成标准准备、项目 maps、run、后处理、结果和报告；两者参考 RMSD `12.760 Å` / `6.422 Å` 均为已知负面姿势诊断，验收器固定 `scientific_success_claimed = false`；
- 上游 v1.2.7 `dry.py` 的只读兼容统计为 7 强、4 弱、7 置换。差异来自上游按 atom name `W` 识别水及半个 spacing 的 map 原点偏移；DockStart 的运行规则按最终类型 `W` 和真实 AutoGrid 原点处理，不把旧脚本输出作为字节 oracle。

## 剩余科学成熟度与后续发布门禁

- 科学优先：增加至少一个预先冻结参考构象、原子映射与 RMSD 阈值并实际通过的独立共晶重对接体系；
- 科学优先：扩展不同配体、口袋水环境、Box/spacing、Vina/Meeko/AutoGrid 版本和失败案例；1UW6 加两套负面姿势诊断不能支持 Stable；
- 工程成熟度：大网格、并发、终止/恢复、maps 替换、迁移和模拟写入失败已有自动化证据；物理磁盘不足与资源上限可在后续硬件基准补测，不再写成项目主链缺失；
- 科学完成后：再做 Basic/Assisted、历史项目、安装/升级/卸载、中文/空格路径、亮暗主题、用户指南、许可证和发布材料。

上述门禁全部满足前，水合 AD4 必须保持 Experimental，不进入默认首页，不与 Stable 结果混排，也不分配正式发布版本号。

---

# 阶段 9：评分权重与开发者接口

## 目标

为专家和开发者提供可控扩展能力，但不干扰普通用户。

## 功能范围

### 9.1 自定义评分权重

支持 Vina、Vinardo、AD4 和 macrocycle glue 权重。

限制：

- 默认关闭；
- 进入实验性专家模式；
- 修改后项目和报告显著标记；
- 自定义分数不与标准权重结果混排；
- 一键恢复官方默认值。

### 9.2 Vina Python API

可选增加：

- Python API adapter；
- notebook 示例；
- 批量分析接口；
- 插件开发接口；
- 只读项目结果 API；
- 不允许插件绕过项目安全边界。

### 9.3 协议插件机制

外部插件可注册输入检查、准备步骤、命令生成、结果解析和报告扩展。插件必须声明工具依赖、许可证、成熟度和数据访问范围，不能默认联网或静默修改原始项目。

---

## 6. 安装包与开箱即用策略

### 6.1 保留双 profile

#### Basic Stable

适合已有 PDBQT 的用户，包含：

- DockStart；
- AutoDock Vina；
- Viewer；
- 标准单配体；
- score/local；
- 已准备输入的批量筛选；
- 手动导入 rigid/flex；
- 手动导入 maps。

#### Assisted Stable

适合从 PDB/CIF/SDF/MOL 开始的用户，包含：

- Basic 全部能力；
- 独立 CPython；
- RDKit；
- Meeko；
- Gemmi；
- 批量配体准备；
- 柔性受体准备；
- 大环准备。

#### Advanced Scientific Components

AutoGrid4、AD4Zn 参数、水合对接工具等只有在分别完成许可证与再分发方案审查后，才可考虑 Assisted 扩展包或可选离线组件包；未完成审查时仅允许用户自行安装/提供并由 DockStart 记录。避免让用户手动配置大量路径不能覆盖 GPL 源码提供、notice 和修改说明义务。

### 6.2 工具管理页面

显示：

- 工具名称；
- 版本；
- 来源；
- 路径；
- 哈希；
- 许可证；
- 适用协议；
- 当前状态；
- 修复方式；
- 重新检测；
- 打开所在目录。

状态语言使用：可用、可选、缺失、版本不兼容、文件损坏、许可证材料缺失、当前协议不需要。

---

## 7. 现代化界面要求

### 7.1 首页

首页只展示：

- 新建项目；
- 最近项目；
- 示例流程；
- 工具状态摘要。

协议分类：

#### 常用

- 标准小分子对接；
- 批量虚拟筛选；
- 姿势评分与优化。

#### 高级

- 柔性侧链；
- 大环；
- AutoDock4；
- 多配体共同对接。

#### 实验性

- AD4Zn；
- 水合对接；
- 自定义权重。

### 7.2 工作区统一结构

- 顶部：当前项目与协议；
- 主区：当前步骤；
- 右侧：输入状态、风险提示、下一步；
- 固定区域：主要操作；
- 3D Viewer 按需显示，不在所有页面常驻。

### 7.3 引导语言

避免只展示参数名，使用用户语言解释：

```text
只评分：对现有姿势计算分数，不执行全局搜索
柔性侧链：允许少量指定残基在对接时改变构象
Affinity maps：AutoDock4 评分所需的预计算网格
```

原始命令放入“技术细节”。

### 7.4 状态优先于配置

页面优先回答：

1. 当前缺什么？
2. 为什么缺？
3. 是否阻塞？
4. 下一步做什么？
5. 需要人工确认什么？

### 7.5 可访问性

状态必须同时使用图标、文字、颜色和可访问标签，不依赖颜色单独表达。

---

## 8. 测试与质量保证

### 8.1 测试层级

#### 单元测试

覆盖参数验证、schema 迁移、配置生成、命令生成、路径安全、maps manifest、结果解析和报告生成。

#### 集成测试

覆盖结构准备、AutoGrid、Vina 执行、任务取消、队列恢复、错误分类和输出归档。

#### 安装态测试

分别验证：

- Basic；
- Assisted；
- 可选高级组件；
- 无网络环境；
- 非管理员安装；
- 中文路径；
- 含空格路径；
- Windows 10；
- Windows 11。

#### 科学回归测试

每种协议至少准备：

- 一个官方或公开基准；
- 一个成功案例；
- 一个明确失败案例；
- 一个边界输入；
- 一个结果解析案例。

### 8.2 发布门禁

```text
后端测试
前端生产构建
cargo check
schema migration tests
Basic package verification
Assisted package verification
installed-state smoke test
protocol scientific regression
license and notices verification
SHA256 generation
```

### 8.3 结果容差

对接算法具有随机性，回归测试不应只比较单个精确分数。建议比较文件生成、pose 数量、合理分数区间、固定 seed 结果、RMSD 范围和关键日志字段。

---

## 9. 风险与控制措施

| 风险 | 影响 | 控制措施 |
|---|---|---|
| 功能堆叠导致界面复杂 | 初学者无法使用 | 协议分类、逐级披露、默认流程不变 |
| 项目 schema 频繁变化 | 历史项目损坏 | 版本化、迁移备份、未知字段保留 |
| 批量任务占满 CPU | 系统卡顿 | 总 CPU 上限、并发限制、暂停/取消 |
| 配体库规模过大 | 前端卡顿 | 分页、虚拟列表、延迟加载 |
| AD4 maps 错配受体 | 科学结果无效 | 哈希绑定、manifest、变更自动失效 |
| 柔性残基选择错误 | 搜索空间失控 | 数量限制、结构检查、显式确认 |
| 大环被误当普通配体 | 结果不可解释 | 自动检测、专用协议、断环记录 |
| 金属体系被过度泛化 | 误用 AD4Zn | 仅限 Zn、人工确认、专用提示 |
| 自定义权重破坏可比性 | 分数误读 | 实验性标签、独立结果、禁止混排 |
| 第三方许可证问题 | 无法合法分发 | 组件级审查、源码材料、notices |
| 运行被中断 | 项目状态不一致 | 原子写入、任务状态机、恢复检查 |
| 高级功能测试不足 | 错误进入 Stable | maturity 分级、实验性隔离、基准门禁 |

---

## 10. 成熟度管理

每种协议标记：

```text
stable
beta
experimental
developer
```

### Stable

完整 UI、文档、自动化测试、安装态测试、科学回归、可恢复运行和清晰报告。

### Beta

主要流程可用，已知限制明确，不进入默认推荐。

### Experimental

独立开关，不承诺结果稳定，不进入默认首页，不与 Stable 结果混排。

### Developer

仅用于调试或插件开发，不面向普通用户。

---

## 11. 基于已打包源码基线与当前工作树的完成路线

旧路线以“高级协议尚未开发”为前提，已经不适用于当前代码。重新规划时必须先承认以下事实：

- 项目到 2026-07-30 尚未发布任何公开 Release。文中历史 `v0.12.0`、`v0.12.2` 与当前本地 `v0.13.0` 都只表示曾使用的源码/打包标识，不能写成已公开发布状态；当前科学工作树不预分配新版本号，在科学功能闭环、实际体验和 UI 优化完成前也不决定首个公开 Release 版本号。
- 当前 `project.json` 仍是 schema v1；柔性受体、大环准备、批量筛选和 AD4 maps 通过向后兼容的扩展字段保存，不能把 v0.12.0 写成“schema v2 已完成”。
- 当前源码同时存在两种明确分开的工作流：“串行批量筛选”让多个配体分别运行；“多配体共同对接（实验性）”让恰好两个成员进入同一次搜索。不得再用“多配体对接”含混指代两者。
- 批量筛选、有限柔性侧链和 Meeko 大环准备已经完成源码主链、UI 接入和真实工具链回归；2026-07-27 证据覆盖 50 配体、取消恢复、1FPU、BACE_1 和标准 AD4。干净 Windows 安装生命周期、重新打包与 GUI 发布材料统一留到科学证据收口和实际体验/UI 优化之后。
- AutoGrid4 maps 管理和 AutoDock4 评分协议已经在历史 v0.12.0 检查点合并为一个可运行闭环，不再拆成两个未来版本。
- 历史 v0.12.2 源码检查点之后的工作已经加入 `score_only`、双阶段 `local_only`、首批高级参数、批量继承六项适用高级参数及批量结果工作区；这些能力不能因为当前源码测试通过就反向写进历史 v0.12.0 安装包，也不能据此声称本地 v0.13.0 已成为公开 Release。
- 当前源码还加入了 Vina/Vinardo 自身的预计算 maps 复用：使用 `--write_maps` 生成，用 `--maps` 运行，并与 AutoDock4 maps 保持独立 manifest、评分函数和报告语义。该增量尚未分配发布版本。
- 当前源码还加入了 AD4Zn beta：TZ 受体准备、用户提供的 `AD4Zn.dat`、AutoGrid4 4.2.7+ 专用 GPF/maps、显式结构确认、不可变 run 证据和独立结果/报告已经接入；1S63、2OI0、1R1J 真机链及失败/恢复矩阵已经完成，但样本量不足以升为 Stable。
- 当前源码还加入了多配体共同对接实验协议：Vina 1.2.0+、恰好两个已准备 PDBQT、刚性受体、Vina/Vinardo 和全局对接；官方 5X72 与 4DM3 多 seed 源码级真机链路已确认，仍保持 Experimental。
- 当前源码还加入了 `hydrated_ad4_experimental`：独立 SDF/MOL 水合配体准备、外部 AutoGrid4 基础 maps、clean-room BEST W map、不可变 run、raw/保留水/去水三类构象产物、逐 Mode 水分类、结果页和报告已接入。固定 1UW6 已通过显式原子映射和 `≤ 2.0 Å` 的正向姿势恢复门槛，Top 3 最佳 Mode 2 为 `0.356053 Å`；2BYS/2ZJU 当前项目 API 真机链覆盖 Meeko、真实 AutoGrid4、W map、Vina、后处理和报告，并继续作为负面姿势诊断。不存在处理后重评分。

### 11.1 不绑定版本号的实际完成阶段

| 阶段 | 当前状态 | 独立验收范围 |
|---|---|---|
| A. 科学主链实现 | 当前范围完成 | 标准 Vina/Vinardo、`score_only`、双阶段 `local_only`、高级参数、串行批筛、有限柔性、大环准备、标准 AD4、Vina/Vinardo 预计算 maps、AD4Zn beta、多配体共同对接和水合 AD4 均已有隔离的数据模型、运行主链、结果语义与 fail-closed 门禁 |
| B. 真实工具链与失败门禁 | 当前收口完成 | 已覆盖 1DWD、1H4W、BACE_1、5X72、4DM3、1S63、2OI0、1R1J、1UW6、2BYS、2ZJU；评价模式、Vina/Vinardo maps 和 BACE 大环独立 verifier 已真实通过，BACE 同时覆盖执行期 Vina 身份与冻结受体/配体输入门禁 |
| C. 规模与成熟度 | 最低闭环完成，继续积累 | 串行批筛已完成 100 项资源基准，当前没有证据要求并行；500 项仅为可选容量 probe。水合与 AD4Zn 仍需更多化学环境和工具版本才能升为 Stable，但这属于成熟度扩展，不是当前功能缺口 |
| D. 实际体验与 UI 优化 | 尚未开始本轮 | 科学阶段完成后再用真实项目逐页体验、截图和修复交互；不在科学闭环期间反复打包 |
| E. 打包与首个公开 Release | 明确延期 | 完成历史项目、Basic/Assisted、安装/升级/卸载、签名、许可证材料和发布门禁后，才决定首个公开版本号；不再预先承诺 `v0.13.0` 或 `v0.14.0` 对应某个协议 |

历史版本字符串只用于定位旧构建与项目记录，不再承担未来交付承诺。首个公开 Release 的版本号由阶段 A～D 的实际完成状态决定。

### 11.2 已进入源码主链但未分配发布版本

`score_only`、`local_only` 与首批七项高级 Vina 参数已完成项目持久化、运行前检查、命令生成、运行快照、结果解析和 Markdown 报告接入；串行批量全局对接也已继承其中适用的六项参数。其中 `local_only` 的第二批定量比较也已完成。这里的“进入源码主链”不等于已发布：

- `score_only` 只评价输入姿势，不产生新构象或排名表；
- 新准备的 `local_only` 先以相同冻结输入与配置运行 `score_only` 基线，再运行 `local_only` 并输出 `optimized.pdbqt`；它仍不是全局对接；
- 结果记录输入与优化后评分、能量项和“优化后－输入”差值，负值不被解释为真实结合改善；
- 姿势位移按严格原子身份在同一受体坐标系直接计算，不做刚体对齐；它不是共晶参考 RMSD，也不生成多构象排名；
- Viewer 可在同一场景安全叠合本次 run 冻结的受体、输入快照与优化后姿势，或单独显示任一姿势；旧单阶段 run 继续可读但不伪造输入基线；
- 帮助页、新建项目页和项目总览已有评分/局部优化入口；文件来源与科学任务正交保存，统一复用现有准备、运行、结果和报告页面；
- 评价模式要求用户在同场 3D 视图确认输入姿势；确认绑定本次运行实际使用的刚性受体、可选柔性侧链与配体 SHA256，替换输入或重建柔性受体后自动失效，并冻结到新 run 的协议快照；
- Vina/Vinardo 可选 autobox，并要求稳定版 Vina 1.2.3 或更高版本；AutoDock4 maps 使用既有 maps 网格，不能同时启用 autobox；
- `max_evals`、`min_rmsd`、`spacing` 与 `verbosity` 按协议和运行模式写入；串行批量全局对接把这些值冻结到队列、逐配体 config、整批报告和归档协议指纹；网格资源保护与详细局部优化日志解析已经接入；
- `no_refine` 与 `force_even_voxels` 默认关闭，仅适用于 Vina/Vinardo；启用后会写入单次 run 或批量队列的不可变配置和报告，并进入批量归档协议指纹；AutoDock4 maps 明确显示不适用；
- `unbound_energy` 默认留空，仅适用于 Vina/Vinardo、刚性单配体 `score_only`；显式 `0` 与留空分开保存，只接受有限数值。新生成的 run 先核对日志 SHA256，再核对第 (4) 项与快照以及 `(1) + (2) + (3) - (4)` 能量平衡；生成后的 `evaluation.json` 在读取与报告前再次核对 SHA256；
- 需要门禁的专家项由当前 Vina 的 `--help_advanced` 精确声明与最低安全版本共同门禁；批量队列启用专家开关时使用相同门禁，并在每次开始或恢复队列前统一复核；每个 attempt 在 Vina 启动前和返回后都会核对本次受体、配体、配置与 Vina 二进制的大小和 SHA256，归档详情及比较继续复核保存的 attempt 输入与配置，但不会复制 Vina 二进制本身。`unbound_energy` 以稳定版 1.2.4 为最低门槛，同核心号预发布版不通过。准备时记录能力证据，执行前再次核对，不能因替换可执行文件绕过；
- 旧项目继续默认 `dock`，不会因打开新版源码而自动改写；
- `randomize_only` 和安装态科学回归仍未完成。

本项尚未绑定新的公开版本号，也没有触发任何公开 Release。当前源码树已经过 Vina 1.2.7 真实双阶段回归，并覆盖基线失败、阶段间取消、关键文件篡改和旧 run 兼容；科学证据收口后还要先完成实际体验与 UI 优化，之后才处理 Basic/Assisted 安装态验证、用户文档和发布材料，并决定首个公开版本号。

Vina/Vinardo 预计算 maps 也已进入同一源码维护线，但它是网格资产与运行语义扩展，不是新评分算法：

- 使用当前 Vina 的 `--write_maps` 生成，或导入 DockStart manifest / 用户确认来源的 raw maps；
- manifest 绑定受体、请求 Box、实际网格、评分函数、Vina binary 与全部 map SHA256；
- 激活前以当前配体运行 `--maps ... --score_only` 兼容性探测；
- prepared run 冻结 maps、manifest、受体溯源快照、配体和配置，执行前后均复核；
- 实际运行不传 `--receptor`，属于 `grid-only / no-refine` 等价语义；
- 当前只允许刚性受体、单配体、全局对接，不与柔性、评价任务、autobox 或批量筛选混用。

2026-07-28 已用随附 Vina 1.2.7 分别完成 Vina 与 Vinardo 的真实生成、复用、结果解析和报告回归。它仍需经过 Basic/Assisted 安装态、历史项目和 GUI 发布门禁，不能据此声明现有 Release 已包含。

AD4Zn beta 也已进入源码主链，但属于另一条显式协议：

- 只接受 Zn，不把其他金属泛化为 AD4Zn；
- 只有恰好三个受体配位方向并存在开放四面体方向时生成 TZ；没有 TZ 时阻止运行，不回退为标准 AD4；
- 使用用户提供的 `AD4Zn.dat`；DockStart 不随仓库/安装包内置或自动下载，用户选择后复制到项目并记录本机来源路径、实际 SHA256、GPL-2.0-or-later、支持配置与固定上游参考；
- AutoGrid4 最低版本为 4.2.7，专用 GPF、GLG 成功状态、maps 和 manifest 必须共同通过；
- Vina 仍使用 `--maps` 与 `--scoring ad4`，协议并未修改 Vina 评分函数；
- 原始/TZ 受体、参数、GPF、maps、工具和用户确认进入不可变 run 证据，结果与报告同标准 AD4/Vina 隔离。

2026-07-30 已完成 1S63、2OI0、1R1J 的真实 TZ → AutoGrid4 → maps → Vina → 结果/报告验收，并覆盖无 TZ、多金属、非 Zn、参数篡改、工具失败和恢复路径。当前科学剩余是扩大不同单核 Zn 几何样本以评估成熟度；历史项目、干净安装和 GUI 属于后续体验/发布门禁。源码存在仍不等于任何公开 Release 已包含 AD4Zn beta。

多配体共同对接也已进入源码主链，但保持 Experimental：

- 最低要求 AutoDock Vina 1.2.0，并确认运行时支持 `--ligand` 多路径输入；
- 只接受恰好两个已准备 PDBQT、刚性受体、显式 Box、Vina/Vinardo 与全局对接；
- 命令只出现一个 `--ligand`，其后按冻结成员顺序传入两个路径；
- 每个输出 `MODEL` 是一组联合构象且只有联合评分，不生成成员 affinity，不直接比较不同成员组合或成员数量；
- Viewer 可用 Mode 只以 `out.pdbqt` 实际存在的 `MODEL` 为准，不能用可能更长的 stdout 结果表补造；
- 不与柔性受体、AD4/AD4Zn/maps、`score_only`、`local_only` 或水合对接组合，不新增第三方依赖或许可证组件。

官方 5X72 案例已于 2026-07-28 用仓库随附 AutoDock Vina 1.2.7 完成源码级真实验收：一个 `--ligand` 后跟两个有序路径、联合解析、双成员 Viewer、评分表与报告均通过；官方参考输出 SHA256 `9fd1901bb52d0c767674fdd6e926f749e9995a35aa5b51e0318fd9e6f6922764` 可解析为 7 个双成员 Mode，最佳联合评分 -19.043 kcal/mol。2026-07-30 的 4DM3 多 seed 验收又覆盖受体电荷、Box、成员总自由度/可旋转键、结果审计、篡改与恢复。4DM3 是补充的稳定性/工程基准，不替代 5X72 的官方命令语义基准；协议仍保持 Experimental。

水合 AD4 Experimental 也已进入源码主链，但不属于标准 AD4 的透明扩展：

- 一个项目内 SDF/MOL 通过兼容 RDKit/Meeko 生成独立水合 PDBQT，要求实际生成 W 类型；标准配体和 raw 输入不被覆盖；
- 外部 AutoGrid4 生成不含 W ligand type 的基础 maps，DockStart 以固定 BEST 规则从 OA/HD clean-room 生成 W map；
- 只允许刚性受体、显式 Box、单配体、全局 AD4 maps 和 Vina 1.2.0+；不与柔性、批量、多配体、Vina/Vinardo maps、AD4Zn、大环 ghost atom、评价任务或 autobox 组合；
- run 冻结受体、水合配体、两个 manifest、全部 maps、配置和 Vina；raw Vina 输出后另设不可取消的 `postprocessing` 阶段，失败保留 raw 文件并终止；
- 成功 run 同时保存 raw、保留强/弱水、去全部 W 和水清单四类证据；Viewer/结果页/报告已经识别该协议；
- raw hydrated AD4 affinity 保持不变，只用于同一 run 内 pose 排序；水分类不产生新分值，报告必须直写“处理后评分未计算”。

固定 1UW6 外部验收已于 2026-07-29 覆盖 Meeko 0.7.1 水合准备、BEST W map、随附 Vina 1.2.7 AD4 maps 运行和 clean-room 后处理：BEST 的 68,921 个值与上游参考逐点一致但规范化文件 SHA256 不同；Vina 请求最多 9 个 Mode 时实际输出 8 个，最佳评分为 `-8.261 kcal/mol`；官方独立 9-pose raw 输出共 18 个 W，按当前运行规则得到 8 强、1 弱、9 置换且 raw affinity 不变。2026-07-30 又以公开 2BYS/2ZJU 完成当前项目 API 的标准准备、真实 AutoGrid4 4.2.7、W map、Vina 1.2.7、后处理、结果和报告全链；两者参考 RMSD `12.760 Å` / `6.422 Å` 均标记为已知负面姿势诊断，因此只证明工程完整性。随后固定 1UW6 共晶参考、显式原子映射和无拟合重原子 RMSD 门禁取得 Top 3 最佳 `0.356053 Å`，完成最低正向科学基准。fixture 继续只提交上游身份、文件 SHA256 和期望元数据，不复制上游结构；更广泛化学/版本矩阵属于 Experimental 向 Stable 演进的成熟度积累，而不是项目级 AutoGrid 主链缺口。

### 11.3 暂不绑定版本号的候选能力

以下能力保留，但在基准、工具链、数据模型和许可证边界明确前，不再提前占用 v0.16–v0.22：

- `randomize_only`、自定义评分权重等其余专家参数；
- 批量 AD4 maps 与柔性受体 AD4；
- 自定义权重、Python API 和协议插件。

水合 AD4 已不再属于“尚未立项候选”，而是**已进入源码、尚未分配发布版本的 Experimental 协议**。它与 AD4Zn、多配体共同对接一样，只有独立完成科学、安装态、GUI、历史项目、许可证和恢复门禁后才可进入版本表；现阶段不得据此承诺某个 v0.x 版本必然发布。

schema v2 也不再作为一个脱离需求的版本目标。只有现有 schema v1 无法安全表达已批准的新协议时，才单独评审迁移；迁移必须提供旧项目备份、只读兼容、失败回滚和跨版本测试，不能为了版本编号而改写用户项目。

每个正式版本仍只完成一个可独立验收的能力闭环。候选能力通过技术验证后再分配版本号，避免用远期编号制造已经承诺交付的错觉。

---

## 12. 开发依赖关系

```text
历史打包基线（未公开 Release）
标准 Vina/Vinardo
+ 串行批量筛选
+ 有限柔性源码主链
+ 大环准备源码主链
+ AutoGrid4 maps / AD4
          ↓
历史源码维护增量（最初形成于 v0.12.1–v0.12.2 检查点，后续进入本地 v0.13.0 基线）
+ score_only / 双阶段 local_only
+ 高级 Vina 参数与能力门禁
+ 批量完整结果、逐配体 Viewer、整批报告、归档只读浏览/比较与单归档 ZIP 导出
+ Vina/Vinardo 预计算 maps 生成、导入、复用、run 前后完整性校验
+ AD4Zn beta 源码闭环（TZ + 外部 AD4Zn.dat + AutoGrid4 4.2.7+）
+ 多配体共同对接实验性源码闭环（Vina 1.2.0+、恰好两个已准备 PDBQT、联合 Mode/评分）
+ 水合 AD4 Experimental 源码闭环（RDKit/Meeko hydrate + 外部 AutoGrid4 + BEST W map + raw/保留水/去水后处理）
          ↓
可重放科学证据收口（当前阶段）
+ score_only/local_only/高级参数与 Vina maps 独立 verifier 已通过
+ 水合 AD4 正向姿势恢复已通过
+ 批筛 100 项资源基准已通过；500 项为可选容量 probe
+ BACE 大环执行期工具与冻结输入门禁已完成真实重放
+ AD4Zn 与水合协议扩大样本作为后续成熟度积累
          ↓
真实项目体验与 UI 优化
          ↓
历史项目、Basic/Assisted、安装生命周期、许可证与签名
          ↓
决定首个公开 Release 版本号和 Stable/Beta/Experimental 集合
```

当前源码增量的完成顺序：

```text
score_only / local_only 源码闭环
+ local_only 双阶段定量比较与真实 Vina 回归
+ 单次及批量 max_evals / min_rmsd / spacing / verbosity
+ 单次及批量 no_refine / force_even_voxels 与运行时能力门禁
+ unbound_energy（刚性单配体 score_only）与日志审计
+ 批量参数冻结、独立 config、完整结果、整批报告、归档只读浏览/比较/ZIP 导出与逐配体输出完整性校验
          ↓
独立真实 verifier 与全仓回归
          ↓
实际体验和 UI 优化
          ↓
Basic/Assisted、用户文档与首个公开 Release 门禁
```

这条分支不表示相关能力已经归属于任何正式版本。schema 演进是跨阶段安全门禁，不再被当成所有功能之前必须完成的独立产品能力。多配体共同对接和水合 AD4 虽已形成彼此隔离的实验性源码闭环，仍必须分别完成各自剩余科学门禁；自定义权重和 Python API 仍需另行立项。任何能力都不能因为批量任务引擎或 AD4 maps 已存在就被视为自然完成。

---

## 13. 分层交付物

### 13.1 科学与源码闭环交付物

每项科学能力在标记为“源码/科学闭环完成”前必须交付：

1. 后端实现；
2. 前端页面；
3. 项目 schema；
4. 参数与科学边界说明；
5. 运行前检查；
6. 日志和错误代码；
7. 结果解析；
8. 报告模板；
9. 自动化测试；
10. 可重放的真实工具链证据与明确失败案例；
11. 数据兼容、失败恢复或安全只读边界。

### 13.2 实际体验与 UI 交付物

科学证据收口后再统一完成：

1. 使用真实项目逐页体验；
2. 亮色/暗色、长中文路径和含空格路径截图复核；
3. 加载、取消、失败恢复和跨页面操作顺序复核；
4. 用户指南、FAQ、示例和界面文案收口。

### 13.3 首个公开 Release 交付物

UI 稳定后才进入：

1. Basic/Assisted 安装态与生命周期测试；
2. 历史项目兼容和迁移/回滚验证；
3. 许可证说明与第三方 notices；
4. CHANGELOG、发布说明和回滚方案；
5. 构建产物、签名与 SHA256；
6. 首个公开 Release 版本号和 Stable/Beta/Experimental 集合决策。

不接受“只有按钮和命令调用，没有结果解释和异常处理”的半成品功能。

---

## 14. Definition of Done

### 14.1 科学与源码闭环 DoD

一个新协议只有满足以下条件，才能标记为“科学与源码闭环完成”：

- 用户可从界面创建该协议项目；
- 输入要求清楚；
- 缺失工具可以检测；
- 结构准备可追踪；
- 参数有默认值与解释；
- 运行前检查可以阻止明显错误；
- 命令使用参数数组构建；
- 每次运行生成独立快照；
- 长时间外部进程可以安全取消，或对不可取消的短暂派生阶段明确边界并 fail closed；
- 异常中断可恢复或明确标记；
- 输出可以解析；
- 结果可在 Viewer 中查看；
- 报告可以导出；
- 科学边界明确；
- 至少有可重放的真实工具链成功案例、明确失败案例和边界输入；
- 自动化测试通过；
- 旧数据兼容、迁移或安全只读边界明确。

### 14.2 公开发布 DoD

“科学与源码闭环完成”不等于已经发布。只有继续满足以下条件，协议才可进入公开 Release：

- 已用真实项目完成逐页体验和 UI 优化；
- Basic/Assisted 支持边界明确；
- 安装包验收及安装、升级、重启、卸载生命周期通过；
- 历史项目兼容与回滚验证通过；
- 用户文档、示例、许可证与第三方材料完整；
- CHANGELOG、发布说明、签名和 SHA256 完整；
- 成熟度标签与首个公开版本号已经根据证据确定。

---

## 15. 最终产品形态

### 第一层：初学者

- 选择标准对接；
- 导入结构；
- 检查提示；
- 设置 Box；
- 使用推荐参数；
- 一键运行；
- 查看结果；
- 导出记录。

### 第二层：科研用户

- 批量筛选；
- 柔性侧链；
- 大环；
- 现有姿势评分；
- AutoDock4；
- maps 复用；
- 结果汇总；
- 科学回归。

### 第三层：专家与开发者

- 多配体；
- AD4Zn；
- 水合对接；
- 自定义权重；
- Python API；
- 协议插件；
- 完整命令和审计信息。

三层能力共享同一套现代化界面、项目管理、运行记录、工具检测、日志体系和结果查看器，而不是形成互相割裂的小工具。

---

## 16. 最高优先级建议

推荐实际开发顺序：

1. 当前科学源码与统一验收已经冻结：后端基线 `1191/1191`、前端 `86/86`、Rust `32/32` 和生产构建通过；后续证据输出预约增量的 BACE/评价 verifier 专项 `20/20` 也通过。评价模式、Vina/Vinardo maps、水合正向基准、100 项批筛与 BACE 大环均有独立真实证据，不再重复列为开发任务。
2. 下一阶段用真实项目逐页体验并优化 UI，按完整任务顺序检查结构获取/转换、协议选择、Box、运行、结果、报告与恢复；截图用于发现交互和排版问题，不替代科学验收。
3. AD4Zn 的更多单核 Zn 几何，以及水合协议的不同水环境、Box/spacing 与工具版本，作为 Beta/Experimental 向 Stable 演进的成熟度工作按新证据增量开展，不阻塞当前功能收口。
4. 100 项资源基准支持当前串行执行；只有明确提出更大容量需求时再运行 500 项 probe，并依据实测决定是否立项并行和单项强制取消。
5. UI 稳定后再处理历史项目、Basic/Assisted、安装/升级/卸载、签名、许可证材料和首个公开 Release，并在那时决定版本号。
6. 批量 AD4 maps、柔性受体 AD4、自定义评分权重、Python API/插件保持候选；`randomize_only` 在固定 seed 可复现问题解决前不作为普通用户功能。

schema 只有在实际数据契约出现阻塞时才升级。本阶段不修改版本号、不重新打包，也不以“发布门禁尚未完成”掩盖仍缺的科学证据。

---

## 17. 总结

DockStart 下一阶段的关键不是“把 Vina 的所有参数全部搬到界面”，而是：

> 用清晰的协议、可靠的准备流程、严格的运行前检查、可恢复的任务系统和可追溯的结果记录，把 Vina 的高级能力整理成真正可用的现代化科研工作流。

整个扩展过程应坚持：

- 默认流程简单；
- 高级能力分层；
- 本地优先；
- 开箱即用；
- 科学边界明确；
- 历史项目安全；
- 每阶段可独立验收；
- 每项功能可验收；
- 不用表面完整代替实际可靠。

最终目标是让 DockStart 从当前的标准对接工作台，逐步成长为覆盖 AutoDock Vina 主要能力、适合初学者与科研用户长期使用的一站式本地分子对接平台。
