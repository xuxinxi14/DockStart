# 2026-06-25 工作日志

## 项目结构分析
- 用户请求分析 DockStart 项目框架和结构。
- 阅读了 PROJECT.md / README.md / CLAUDE.md / AGENTS.md、backend 核心模块、apps/desktop 前端、Tauri 桥接层、docs/roadmap.md。
- 绘制了三层架构 SVG（前端 React → Tauri Rust 桥接 → Python 后端 → 持久化）。
- 识别出几个观察点：workflows/ 为空、main.rs 用 python/python3 而非 settings 配置的 python_path、tauri.conf.json 的 CSP=null、前端无路由库、无 LICENSE 文件。

## 为所有路径输入增加"选择路径"按钮
- 用户要求：所有需要输入路径的位置增加选择按钮，点击打开原生对话框选路径，并把规范写入 CLAUDE.md。
- 定位 6 处路径输入：ProjectCreatePage(baseDir 目录)、ImportPdbqtPage(receptor/ligand .pdbqt 文件×2)、SettingsPage(vina/python 可执行文件×2、default_project_dir 目录)。
- 技术方案：接入 Tauri 2 官方 tauri-plugin-dialog 插件。
  - Rust：Cargo.toml 加 tauri-plugin-dialog="2"；main.rs 注册 .plugin(tauri_plugin_dialog::init())。
  - 权限：新建 src-tauri/capabilities/default.json，授权 core:default + dialog:default，作用于 main 窗口。
  - 前端：package.json 加 @tauri-apps/plugin-dialog ^2.0.0。
  - 组件：新建 apps/desktop/src/components/PathInput.tsx，封装"输入框 + 选择…按钮"，支持 mode=file/directory、filters、title；调用 open()，取消时静默，非 Tauri 环境下 try/catch 忽略。
  - 样式：styles.css 追加 .path-input/.path-input-field/.path-input-button，flex 布局适配 setting-row 的 grid。
- 三页面 6 处全部替换为 PathInput：PDBQT 文件带 filters=[{name:"PDBQT",extensions:["pdbqt"]}]。
- 文档：
  - CLAUDE.md §7.1 新增"路径输入必须提供选择按钮"规范。
  - docs/license_notes.md 同步新增 tauri-plugin-dialog 依赖条目（按 §10 合规）。
- 验证：npm install 成功（新增 1 包）；npm run build（tsc + vite build）通过，44 模块无类型错误；cargo build 后台编译中。

